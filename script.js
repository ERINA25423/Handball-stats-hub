// ========================================
// Handball Stats Hub
// Main Script
// ========================================

window.APP = {
    version: "0.9.0"
};

document.addEventListener("DOMContentLoaded", () => {
    console.log("Handball Stats Hub v" + APP.version);
});
